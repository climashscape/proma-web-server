{
  "name": "proma-web-server-poc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "test": "bun run client-test.ts",
    "build:web-preload": "bun run web/build-web-preload.ts && bun run web/build-preload-bundle.ts",
    "smoke:browser": "bun run browser-smoke.ts"
  },
  "description": "Proma Web 服务化阶段 1 PoC：Bun HTTP + WebSocket IPC 桥"
}
