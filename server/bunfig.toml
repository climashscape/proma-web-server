# Proma Web PoC — bunfig
# 说明：bunfig [alias] 在此环境（Bun 1.3.14 / WSL）实测不生效，
# 实际 electron stub 通过替换 node_modules/electron/index.js 实现（见 handoff §五/§六）。
# 保留此文件仅为未来版本升级后重试 alias 机制预留。
[install]
save = false
