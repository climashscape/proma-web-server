# Proma Web PoC — bunfig
# 说明：electron stub 通过 patches/patch-proma.sh 源码级 patch 实现
# （把 from 'electron' import 替换为 @proma/electron-stub 并建立 node_modules 链接）。
# bunfig [alias] 机制在此环境（Bun 1.3.14）实测不生效，保留此文件仅为未来版本升级后重试 alias 机制预留。
[install]
save = false
